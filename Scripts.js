window.onload = function() {
    // Text fades in and out automatically with CSS animation.
    
    // Wait for the text animation to finish (4 seconds)
    setTimeout(function() {
        // Start ballerina animation (already set in CSS)
    }, 4000);
};

